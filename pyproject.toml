[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "genja"
version = "22.10"
dependencies = [
    "markdown",
    "jinja2"
]

[project.scripts]
genja = "genja.main:main"